.container
	h2.main-header КОНТАКТЫ
	a(href="#/contact/new").main__menu-addButton Добавить
	.content-container
		table(class="contacts-list table")
			thead.contacts-list__head
				tr
					th #
					th Имя
					th Фамилия
					th Телефон
					th Email
			tbody
				tr(ng-click="go('/contact/'+contact.$id)" ng-repeat="contact in contacts" class='contacts-list__item')
					td {{$index+1}}
					td {{contact.name}}
					td {{contact.lastname}}
					td {{contact.tel}}
					td {{contact.mail}}